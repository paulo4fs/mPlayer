<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PlayMusic" parent="Theme.MaterialComponents">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorLightBlue</item>
        <item name="colorPrimaryVariant">@color/colorDarkBlue</item>
        <item name="colorOnPrimary">@color/colorGray</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/colorGray</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="editTextStyle" parent="Widget.Design.TextInputLayout">
        <item name="backgroundTint">@color/white</item>
        <item name="cornerRadius">@dimen/dim10sp</item>
        <item name="background">@color/colorLightBlue</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="playButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">@dimen/dim100dp</item>
    </style>

    <style name="buttonStyle" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">@dimen/dim25dp</item>
    </style>

    <style name="robottoTextStyle" parent="android:Widget.TextView">
        <item name="fontFamily">@font/roboto</item>
        <item name="android:textColor">@color/white</item>
    </style>


</resources>